name: flutter_sticky_header
description: Flutter implementation of sticky headers as a sliver. Use it in a CustomScrollView.
version: 0.7.0
homepage:

dependencies:
  flutter:
    sdk: flutter
  # value_layout_builder: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: '>=2.12.0-0 <3.0.0'
  flutter: ">=1.20.0 <2.0.0"
